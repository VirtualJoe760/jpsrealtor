---
title: Tech Debt / Known Issues
status: current
last_verified: 2026-06-09
---

# Tech Debt / Known Issues

Live list of known platform tech debt. Sorted by severity, then chronological.
This is the canonical place to look before assuming something is "broken vs. by design."

Severity legend:
- 🔴 **critical** — active bug, security risk, or data integrity
- 🟠 **high** — real ongoing pain, frequent footgun
- 🟡 **medium** — annoying, papers-over-able
- 🟢 **low** — cleanup, cosmetic, eventual

---

## 1. Active issues

| Severity | Issue | Location | Impact | Recommended fix |
|---|---|---|---|---|
| 🟠 high | `vercel.json` catch-all caches every path as `public, max-age=31536000, immutable` — including `/api/*` | `F:\web-clients\joseph-sardella\jpsrealtor\vercel.json` (the `/(.*)` rule) | Any dynamic API route that forgets to set its own `Cache-Control: no-store` will freeze per-user / per-domain data in browser/CDN caches for a year. Recurring class of bug. (`/api/insights/*` patched 2026-06-05 — see Resolved.) | Scope the catch-all to `/_next/static/*` and `/(.*\.(js\|css\|woff2\|png\|jpg\|svg\|webp\|ico))$`. Add a sane default for HTML (`s-maxage=60, stale-while-revalidate=300`). |
| 🟠 high | Most public-facing API routes still scope content by `session.user.id` instead of the domain owner | `F:\web-clients\joseph-sardella\jpsrealtor\src\app\api\` (audit pending) | Class of bug. Visitor sees the wrong agent's content on apex domains, or empty pages, or leaked content from another tenant. Bethany-article-on-jpsrealtor (May 18) was one instance. | Grep every API route under `src/app/api/`, classify each as agent-private (correctly uses `session.user.id`) vs public-facing (must use `resolveDomainOwner`), then migrate the public-facing ones. Reference impl: `/api/articles/list/route.ts`. |
| 🟠 high | `AGENT_DOMAIN_MAP` in `src/proxy.ts` is hardcoded empty | `F:\web-clients\joseph-sardella\jpsrealtor\src\proxy.ts:18-20` | Custom agent domains currently get content resolution via `DomainRegistry` lookup inside `resolveDomainOwner`, but the proxy's static map never reads from it. If a custom domain ever needs URL rewriting at the proxy layer (e.g. `/` → `/agent/{id}`), it'll silently miss. | Either dynamic-load from `DomainRegistry` at edge (with KV cache) or document explicitly why the static map is intentionally unused. |
| 🟠 high | `src/proxy.ts` hardcodes `http://localhost:3000/` as the non-admin redirect on `agent.chatrealty.io` | `F:\web-clients\joseph-sardella\jpsrealtor\src\proxy.ts:106` | In production, real users who hit `agent.chatrealty.io` without admin get sent to localhost. Silent failure for anyone external. | Use `new URL("/", request.url)` (request origin) or env-aware `process.env.NEXTAUTH_URL`. |
| 🟡 medium | `Campaign` model declares the `dropCowboyConfig.campaignId` index twice | `F:\web-clients\joseph-sardella\jpsrealtor\src\models\Campaign.ts:284` and `:513` | Mongoose logs a noisy "Duplicate schema index" warning on every cold start. Confuses log scanning; suggests the index might double up. | Remove either the field-level `index: true` (the `:284` declaration is implicit via schema, not present here — check) OR the explicit `schema.index({ 'dropCowboyConfig.campaignId': 1 })` at the bottom. Keep one. |
| 🟡 medium | NextAuth JWT can't be revoked | `F:\web-clients\joseph-sardella\jpsrealtor\src\lib\auth.ts` (`session.strategy: "jwt"`) | A stolen token (XSS leak, dev clipboard, etc.) remains valid until its 30-day expiry, even after the user clicks sign-out everywhere. Cookies clear; the JWT lives. | Eventual: move to database sessions (`strategy: "database"`). Requires a `Session` collection and revisiting cross-domain transfer/receive. Not urgent unless we see incident traffic. |
| 🟡 medium | Article `author.id` orphan pattern can recur | `F:\web-clients\joseph-sardella\jpsrealtor\src\models\Article.ts`; publish path under `src/lib/publishing-pipeline.ts` | May 19 backfill (`b97bfdcf`) fixed 39 articles whose `author.id` pointed at a stale Joseph `_id`. Could recur if Joseph's `User._id` ever changes again (account delete + recreate, manual data rewrite, migration). | Publish endpoint should always stamp `author.id` from current `session.user.id`, not from a snapshotted field. Add a once-a-day cron that flags any article whose `author.id` doesn't resolve to a `User`. |
| 🟡 medium | Several legacy `/docs/*` docs reference `src/middleware.ts` which doesn't exist | various under `F:\web-clients\joseph-sardella\jpsrealtor\docs\` | The file was renamed to `src/proxy.ts` in Next.js 16. New contributors following the old docs grep for nothing. | Documentation refactor in progress under `docs-v2/`. Old `/docs/` to be archived as drift cleanup proceeds. |
| 🟡 medium | `NEXTAUTH_SECRET` is a single point of failure for the whole multi-apex network | `.env` / Vercel project env | Anyone who obtains it can mint a `/api/auth/transfer` JWT for any email in `ALLOWED_DOMAINS` and become any user on any platform apex. Effectively root for the platform. | Rotate aggressively if leaked. Eventual: per-apex transfer secrets, or move to short-lived asymmetric signing keys (KMS). |
| 🟡 medium | In-memory rate limiter doesn't survive across serverless instances | `F:\web-clients\joseph-sardella\jpsrealtor\src\lib\rate-limit.ts` | Vercel serverless functions cold-start fresh; an attacker who spreads requests across instances sidesteps the per-IP / per-email window. Mitigation today: traffic is low and most attacks fail Turnstile first. | Eventual: KV-backed (Upstash Redis works on Vercel edge runtime). Move the sliding-window logic behind a `RateLimitStore` interface so swap is trivial. |
| 🟢 low | Three "master" docs in `/docs/architecture/` predate Next.js 16, chat-v3, multi-tenant, ChatRealty | `F:\web-clients\joseph-sardella\jpsrealtor\docs\architecture\MASTER_SYSTEM_ARCHITECTURE.md`, `DATABASE_MODELS.md`, `FRONTEND_ARCHITECTURE.md` | All dated Jan 2025. Misleading for anyone (or any Claude) onboarding. | Rewrite as area docs under `docs-v2/` (cms/, listings/, chat/, etc.). Move originals to `docs-v2/archive/` when superseded. |
| 🟢 low | Mongoose strict mode silently drops unschema'd fields on save | all `src/models/*.ts` | When you add a field to an API payload or UI form without adding it to the Mongoose schema, the field just disappears at write time with no error. Frequent gotcha when iterating on a new feature. | Mitigation: `docs/MONGOOSE_SCHEMA_GUIDE.md` exists; add a checklist item to PR template — "If touching a model, sync schema → interface → API → UI." |
| 🟠 high | Existing OAuth / API credentials are stored **plaintext** in Mongo | `User.calendarSettings.refreshToken`, `User.adAccounts.google.{developerToken,refreshToken}`, `User.adAccounts.meta.{accessToken,pageAccessToken}` in `F:\web-clients\joseph-sardella\jpsrealtor\src\models\User.ts` | A database compromise or careless `find().lean()` log dump exposes every connected agent's third-party tokens. The new Anthropic BYOK feature (June 2026) ships its own encryption helper (`F:\web-clients\joseph-sardella\jpsrealtor\src\lib\secrets.ts`) — these older credentials should be migrated to use it. | Add `encryptSecret` / `decryptSecret` wrappers around the relevant read/write call sites (gcal-api.ts, google-ads-api.ts, meta-ads-api.ts). Backfill existing rows with a one-shot script that encrypts in place. Requires `SECRETS_ENCRYPTION_KEY` in all environments. |
| 🟠 high | `DomainRegistry` model writes to collection `domain_registry`, but `resolveDomainOwner` reads `domainregistries` (a different collection) | `F:\web-clients\joseph-sardella\jpsrealtor\src\models\DomainRegistry.ts:285` (`collection: "domain_registry"`) vs `F:\web-clients\joseph-sardella\jpsrealtor\src\lib\resolveDomainOwner.ts:100` (`db.collection("domainregistries")`) | The admin approval/provisioning flow saves a newly-approved custom domain into `domain_registry`, but the host→owner resolver queries `domainregistries`. A custom domain may therefore fail to resolve to its agent's site (it only works if `agentProfile.customDomain` is also set, which is the earlier resolver step). As of 2026-06-08: 1 doc in `domain_registry`, 4 stale docs in `domainregistries`. | Pick one canonical collection, point BOTH the model and the resolver at it, and migrate the legacy `domainregistries` docs across. Verify custom-domain → owner resolution end-to-end after. |
| 🟡 medium | Cloudflare custom-domain provisioning needs an **account-scoped** token with `Zone:Create`; the configured token lacks it | `CLOUDFLARE_API_TOKEN` consumed by `F:\web-clients\joseph-sardella\jpsrealtor\src\lib\domain-registry\cloudflare-provision.ts` | On domain approval, Cloudflare zone creation 403s ("Requires permission com.cloudflare.api.account.zone.create"). The domain still goes live via the Vercel CNAME, but the Cloudflare caching / page-rule / worker-route layer never provisions. As of 2026-06-08 the approve flow now **persists** the failure (`DomainRegistry.cloudflare.{status:"failed", lastError, lastAttemptAt}`) and surfaces it in the admin Registry tab (red "failed Cloudflare provisioning" banner + Retry button + red ⚠ in the Cloudflare column), instead of the error silently vanishing once the row leaves the Pending tab. | Grant the CF API token **Account · Zone · Create** (account-scoped, not zone-scoped) in the Cloudflare dashboard, then use "Retry Provisioning". Confirm whether prod uses a different, correctly-scoped token than dev. |

---

## 2. Resolved issues (chronological)

Recent fixes from the May 2026 work. Format: `date — sha — one-liner`.

| Date | Commit | Fix |
|---|---|---|
| 2026-05-15 | `ced6d7ab` | Cloudflare Turnstile + per-IP/per-email rate limit added on auth and lead forms; killed the bot-signup spam wave. |
| 2026-05-15 | `61230328` | Meta Pixel suppression when URL contains lat/lng — avoids leaking buyer location to Meta on map deep-links. |
| 2026-05-17 | `0abb57e1` | Chained multi-domain signout — clicking sign-out on any platform apex now clears cookies on all of them, not just the current one. |
| 2026-05-17 | `186e9d89` | Signout-chain endpoint emits **two** real Set-Cookie headers via `headers.append()` — was previously calling `cookies.set()` twice with the same name, which overwrites; only one cookie variant was being cleared. |
| 2026-05-18 | `f04682bb` | `www.chatrealty.io` now rewrites to `/chat-landing` — old check was `!bareHost.includes(".chatrealty")` which is false for www, so www traffic was misrouted as a subdomain. |
| 2026-05-18 | `b891a509` | Agent branding endpoint resolves apex domains via `resolveDomainOwner` (was scoping by session — failed for unauthenticated visitors on `jpsrealtor.com`). |
| 2026-05-18 | `9f11c0e7` | `/api/articles/list` scopes by domain owner, not visitor session. Fixed the Bethany-Klier-article-showing-up-on-jpsrealtor bug. |
| 2026-05-19 | `b97bfdcf` | Backfilled `author.id` on 39 articles whose Joseph reference pointed at a stale `User._id`. |
| 2026-05-19 | `2633edac` | Inlined local image paths in MDX + migrated Mongo article images to Cloudinary; deleted one dead post. |
| 2026-05-19 | `b3321103` | Moved GSC service-account credentials out of project root into `src/.credentials/` so they stop appearing in dir listings. |
| 2026-05-19 | `ef2000be`, `375bd0a0` | Root directory cleanup — removed ~1.27 GB of stale root-level artifacts and rotated SSH keys. |
| 2026-05-21 | `106d0b9c` | docs-v2/ foundation + first 3 priority area docs (routing, auth, multi-tenant). |
| 2026-06-05 | _(pending)_ | `/api/insights/community-spotlight` + `/api/insights/favorite-spotlight` now stamp `Cache-Control: no-store` on every response (handler wrapper) and get a dedicated `/api/insights/(.*)` no-store rule in `vercel.json`. These render the homepage's per-user "Favorites Spotlight" + "Real Estate in {city}" sections from `swipeAnalytics`/`likedListings`; previously they inherited the immutable catch-all and could leak one user's favorites/preferred areas into a shared CDN node. |
| 2026-06-05 | _(pending)_ | Article search highlighter (`ArticleCard.tsx` + `ArticleAccordion.tsx`) rewritten to split text and wrap matches in real `<mark>` React nodes instead of regex-replacing into `dangerouslySetInnerHTML`. The old code re-scanned its own injected markup, so any search term that is a substring of the injected `class="…"` (notably **"la"** → "La Quinta", a core local query) shattered the tag and leaked raw HTML into article titles. Fix also escapes the term (regex-injection safe) and removes the XSS surface. |
| 2026-06-05 | _(pending)_ | `/api/agent/public` was fetched 4× per homepage load — three independent client consumers (`page.tsx`, `ClientLayoutWrapper.tsx`, `useAgentProfile.ts`) plus React StrictMode double-invoke, none sharing a cache. Added a memoized `fetchAgentPublic()` in `useAgentProfile.ts` that dedupes the in-flight request per URL; all three consumers now share one call (verified 4 → 1). Also removed the dev-mode 503s that the concurrent first-compile requests were triggering. |
| 2026-06-09 | _(pending)_ | **Partner (service-partner) approval is now a real gate.** Previously `/api/service-partner/apply` granted the `serviceProvider` role + created the directory profile immediately, and the public directory (`/api/service-partner/directory`) filtered only on `roles: serviceProvider` — so any applicant was **publicly listed with zero review**, and the admin "Partner Applications" Approve button was a no-op (the code literally said "could add a status field in the future"; only Reject did anything). Added `servicePartnerProfile.status` (`pending`/`approved`/`rejected`) + `appliedAt`/`approvedAt`/`approvedBy`/`rejectionReason`. Apply now sets `pending`; the directory filters `status ∈ {approved, null}` (`null` = grandfathered legacy partners, 0 at rollout); admin Approve sets `approved` (Reject sets `rejected` and drops the role); the admin page shows a status badge + context-aware Approve/Revoke/Re-approve buttons. Verified end-to-end on localhost: a pending partner is hidden from the directory, then appears immediately after admin approval. |
| 2026-06-09 | _(pending)_ | **Partner moderation: admin auto-approve toggle + 24h backstop + emails.** Building on the gate above: added a platform-wide `PlatformConfig._id="moderation".moderation.partnerAutoApprove` toggle (default ON), surfaced as a switch on the admin Partner Applications page (API: `/api/admin/settings/partner-approval` GET/PUT). When ON, `/api/service-partner/apply` approves on submit; when OFF, applications stay `pending` and a **24h cron backstop** (`/api/cron/auto-approve-partners`, hourly via `vercel.json` crons; auth via `CRON_SECRET` **or** the existing `INTERNAL_API_SECRET`) approves anything left untouched. Approval/rejection now email the partner (`sendPartnerApprovalEmail` / `sendPartnerRejectionEmail` in `email-resend.ts`); shared logic lives in `src/lib/partner-moderation.ts` (`approvePartner`/`rejectPartner`) so apply, admin, and cron stay consistent. Verified end-to-end: cron approved a 25h-old pending partner and the approval email sent (Resend id returned). |
| 2026-06-05 | _(pending)_ | Map city-stat cards showed all-property-type data mislabeled as sale. The city-clustering path in `src/app/api/map-clusters/route.ts` built clusters from the pre-aggregated `City` collection (all-types `listingCount`/`priceRange`) and hardcoded `propertyTypes: ['A']`, ignoring the `listingType` filter — so La Quinta showed **898 listings / $1.1K–$85M** (the $1.1K floor was a rental's monthly rent) instead of **506 sales / $100K–$85M**. Rewrote the path to compute per-city stats live via a `$group` over the property-type-filtered `matchStage` (same pattern the grid-clustering path already used), keeping curated city-center coords for placement. Verified across La Quinta/Indian Wells/Coachella; valley total 5,652 → 3,388. **Residual data-quality tail (separate):** a few listings are mis-typed `propertyType=A` (sale) but priced like rentals (e.g. listingKey `20241106010452172739000000`, 77955 Calle Las Brisas South Palm Desert, $795) — these slip the filter because the DB record itself is mislabeled; needs source/ingestion correction, not a route change. |
| 2026-06-05 | _(pending)_ | Mobile iOS Safari auto-zoomed the viewport when focusing the map search/filter inputs (font-size < 16px) and broke the fixed-layout `/chap` map. Added a global `@media (max-width:767px)` rule in `globals.css` forcing form controls to 16px (`!important` to beat `text-xs`/`text-sm`). Verified on a real 500px viewport: the filter inputs/selects now compute 16px. Also fixed a separate bug in the `/mls-listings` `FiltersPannel.tsx` — 17 classNames interpolated `${inputBg}` etc. inside plain double-quoted strings (never applied; inputs rendered unstyled) plus a garbage `'${inputFocus}'` dark default; converted to template literals. |
| 2026-06-05 | _(pending)_ | PWA manifest fixes: `manifest-v2.json` had white `theme_color`/`background_color` on a black-default app (white splash flash) → set both to `#000000`; removed `screenshots` entries that pointed at missing files (`/screenshots/home.png`, `/screenshots/mobile-home.png` — 404 in install UI). **Still open:** manifest name/short_name/description are hardcoded to Joseph Sardella (multi-tenant domains show his branding — needs a dynamic per-owner manifest route). |
| 2026-06-05 | _(pending)_ | **Push notifications were silently broken in production.** The only `push`/`notificationclick` handlers lived in the hand-written `public/sw.js`, which is gitignored and dev-only. In production `next-pwa` (`dest:"public"`) generates a Workbox SW that OVERWRITES `public/sw.js` with caching-only logic — and there was no `worker/index.js` (the file next-pwa 5.x auto-bundles into the generated SW), so the push handlers never shipped to prod. Added `worker/index.js` with the push/notificationclick/message/sync handlers (badge repointed to the existing `/icons/icon-72x72.png`); next-pwa now imports it so prod gets Workbox caching AND push. Note: can't be exercised in dev (next-pwa is `disable`d in development), so the hand-written `public/sw.js` still serves locally. |
| 2026-06-05 | _(pending)_ | **Chat aggregate/market-stats returned "no active listings" for cities full of listings**, e.g. "average home price in Indian Wells" / "Palm Desert". Root cause: `query-parser.ts` emits human propertyType words ("house"/"condo"/"home") but `buildListingQuery` used `filters.propertyType` verbatim as the MLS `propertyType` field (which is the letter code A/B/C/D), so "home"→`propertyType:"house"` matched zero docs. ("homes for sale" worked only because `\bhome\b` skips the plural.) Added `normalizePropertyType()` in `listing-query.ts` mapping every convention — letter codes, residential subtypes (house/condo/townhouse), and listing-type words — onto `{propertyType code, propertySubType}`: house/home→A (all residential), condo/townhouse→A+subtype, rental/lease→B (monthly rent), multi-family/duplex→C, land→D. Also taught `query-parser.ts` to detect rental/multi-family/land (conservative patterns so the local "Indian **lease land**" term isn't mistaken for a rental). Verified: Indian Wells 0→146, and "average rent in La Quinta"→412 rentals with monthly-rent stats (new capability). Minor cosmetic follow-up: the StatsCard header shows "TYPE HOUSE" for generic "home" queries. |
| 2026-06-05 | _(pending)_ | Dead `/newsletter-signup` link (sidebar quick action in `EnhancedSidebar.tsx`) → 404. Replaced with a native newsletter backend (Resend + Mongo, domain-scoped): `NewsletterSubscriber` + `Newsletter` models; `subscribe` (Turnstile + rate-limit + `resolveDomainOwner`), `unsubscribe` (one-click GET + RFC 8058 POST), `subscribers`, issue CRUD, and Resend `send` routes; plus the public `/newsletter-signup` page. v1 is backend + signup only — CMS composer + AI-generated drafts are a planned follow-up. See `docs/cms/README.md` § Newsletter. (Audit also confirmed every other internal link resolves — `/sitemap` was a regex artifact of `/sitemap.xml`.) |
| 2026-06-05 | _(pending)_ | **Off-market listings showed "no photo" even though their photos were synced.** `/api/listings/[listingKey]/photos` (the detail-page photo fetch) `.select(...)`-ed a non-existent `photos` field and ALWAYS fetched live from the Spark Replication API, ignoring the synced `unified_listings.media[]`. When a listing goes off-market it drops out of the live Spark feed (`Results: []`), so the route returned zero photos. Rewrote it DB-first: serve `media[]` → fall back to live Spark → `primaryPhotoUrl` last resort, with a `source` field on the response. Verified: the off-market Naples listing (`20260226130344914839000000`) went 0 → 31 photos (`source: "db"`) and the detail page now renders them. **Also fixed the shared `src/lib/listings/fetch-primary-photos.ts`** (card/list/chat photo helper — used by `/api/cities/[cityId]/listings`, `/api/listings/nearby`, `/api/listings/related`, and `chat-search/preview.ts`): it was Spark-only too, so off-market card photos broke. Now reads `media[]`/`primaryPhotoUrl` from Mongo and Spark-fetches only the keys the DB can't resolve (also cuts Spark traffic). |
| 2026-06-06 | _(pending)_ | **Hydration error on MDX pages: `<p>` cannot be a descendant of `<p>`.** Landing-page/article MDX content (in Mongo) sometimes has a literal `<p className="text-xs …">disclaimer</p>`; MDX re-wraps the inner text in its own `<p>`, producing `<p><p>…</p></p>` → invalid HTML → React hydration error (seen on `/lp/medpro-*`). Fixed at the renderer with a dependency-free rehype plugin `src/lib/mdx/rehype-unwrap-nested-p.ts` that unwraps a `<p>` nested directly in a `<p>`, wired into both `MDXRemote` renderers (`/lp/[slug]` and `/insights/[category]/[slugId]`). Renderer-level so the whole class of pages is fixed, not one document. **Key gotcha:** a literal `<p className=…>` in MDX is an `mdxJsxFlowElement` (`name:"p"`), NOT a hast `element` (`tagName:"p"`) — the first version only matched `tagName` and missed the literal wrapper, so the bug persisted; the plugin now treats both representations as a paragraph. Verified by compiling the disclaimer through `@mdx-js/mdx`: without the plugin → `_jsx("p",{children:_jsx(_components.p,…)})` (nested); with it → `_jsx("p",{children:"…text…"})` (flat). NOTE: dev renders landing pages from the **disk** `src/posts/*.mdx` (which lacks the disclaimer); the nesting only appears in Mongo content (prod), so verify via compile/prod, not the dev page. |
| 2026-06-05 | _(pending)_ | **Chat parser too narrow / brittle** (probed via `/api/test-chat/parse`). Fixes in `query-parser.ts`: (1) `parsePrice` decimal bug — "1.5M" parsed as **$15M** (it stripped the dot) → now $1.5M; (2) word-form prices — "a million", "half a million", "750 thousand" now parse; (3) budget triggers — "for 500k", "around $600k", "budget of 800k", "what can I get for 500k" now set `maxPrice` (was dropped entirely); (4) CMA recognition broadened — "what's my home worth", "how much is my house worth", "sell my home", "value of my home" now route to `cma` (were `conversational`); (5) **entity bug** — "best **neighborhoods**" fuzzy-matched city **"Hood"**; generic words (neighborhood/community/area/place/subdivision) are now stripped before entity resolution; (6) "best neighborhoods for families" / "where should I live" → `insights` instead of a listing search. Verified before/after across 15 queries, no regressions. **Still narrow:** a location-less listing search ("find me something under a million with a view") still falls to `conversational`/Layer 3 (filters now extract, but no scope) — a future enhancement could run it against the map viewport or a default market. |
| 2026-06-05 | _(pending)_ | **Chat listing-search sort was hardcoded price-descending** — so "cheapest homes in La Quinta" showed the $85M estate first. Added `sort` + `maxDaysOnMarket` to `ListingFilters`; the parser now maps "cheapest/cheap/affordable/best deal" → `priceAsc`, "most expensive/luxury/priciest/high-end" → `priceDesc`, "newest/latest" → `newest` (onMarketDate), and "just listed/new this week" → `maxDaysOnMarket=7` ("this month" → 30). `buildListingQuery` applies the recency window (active `onMarketDate`), and `preview.ts` listing-search uses the sort spec. Verified end-to-end: "cheapest" → [240k,250k,259k…] ascending; "most expensive" → [85M,29M…] descending. (New features, not just a fix.) |
| 2026-06-05 | _(pending)_ | **Off-market favorites were removed silently — no notification.** `/api/user/favorites/sync-status` (run once per session by `useFavoritesSync` on login) already unfavorites listings whose MLS status is no longer Active (or that vanished from `unified_listings`) and archives them to `User.statusChangedListings`, and dispatched a `favoritesStatusSync` window event — but nothing listened to it, so the user never knew. Added a `react-toastify` notification in `useFavoritesSync` (infra already mounted in the layout): e.g. "78715 Naples Drive came off the market and was removed from your favorites" (phrasing adapts to sold/pending/under-contract). Verified end-to-end with a simulated off-market favorite. **Follow-up:** the `statusChangedListings` archive isn't surfaced in any UI yet — a notification-history view could use it; and the toast only fires on next login (a server-side cron could push/email proactively). |
| 2026-06-05 | _(pending)_ | **Duplicate favorites inflated the favorites count + dashboard analytics.** The bulk-sync `POST /api/user/favorites` mapped the client's list and REPLACED `likedListings` with no dedupe, so duplicate `listingKey`s accumulated (Joseph had 21 raw / 13 unique). The favorites GET dedupes for display (13) but the dashboard count and `calculateAnalytics()` (top cities/subdivisions, swipe insights that also drive the homepage spotlight) counted the raw array — La Quinta showed 9 instead of 5. Added a `listingKey` dedupe in the sync POST: prevents new dupes and self-heals the stored array on the next sync. Verified by re-syncing Joseph's 13 unique favorites — raw array 21→13, La Quinta analytics 9→5. (The single-add `[listingKey]` route already guarded against dupes; only the bulk sync didn't.) |

---

## 3. Aspirational / not-yet-implemented

Things in `/docs/` planning files that haven't actually shipped. Source-of-truth: read the planning doc, then verify against `src/`.

| Feature | Source doc | Shipped? | Notes |
|---|---|---|---|
| Multi-tenant 24-week build (Weeks 1-24) | `F:\web-clients\joseph-sardella\jpsrealtor\docs\multi-tenant\index.md` | partial | Weeks 1-6 (data layer, asset upload, agent dashboard) substantially shipped (`User.agentProfile`, asset uploads via Cloudinary, agent settings wizard). Weeks 7-12 (landing-page transformation, custom backgrounds, subscription gates) partially shipped — landing page exists but custom themes and gate enforcement are partial. Weeks 13+ (representation agreements, agent matching, team features, territory/fee tracking) **not shipped.** |
| Tiered user subscriptions (Anonymous → Free → Pro $10 → Ultimate $20 → Investor $99) | `docs\multi-tenant\index.md` § "User Subscription Tiers" | no | Current Stripe product set is **agent** subscriptions (Beginner $125 / Experienced $500 / Top Agent $1000), not consumer tiers. The consumer ladder hasn't been built. |
| Agent-sponsored client subscriptions (deferred billing) | `docs\multi-tenant\index.md` § "Agent-Sponsored Subscriptions" | no | Concept only. No `SponsoredSubscription` model, no deferred-billing job, no agent UI to sponsor a client. |
| Representation Agreement model (30-day rolling, unlocks showing/offer/messaging) | `docs\multi-tenant\index.md` § "Representation Agreements" | no | No `RepresentationAgreement` model. Agent-client linkage today is implicit via `Contact.assignedAgentId`. |
| Data-broker fee distribution (5% to MLS contributor, automatic) | `docs\multi-tenant\index.md` § "Data Broker Fee" | no | Tracked conceptually in `agentProfile.mlsDataSources` but no payout pipeline. |
| Referral fee automation (15-25% out-of-area / swipe match) | `docs\multi-tenant\index.md` § "Referral Fees" | partial | Manual FUB-driven workflow exists. Automatic detection + Stripe Connect split is not built. |
| 8-agent React Native mobile app | `F:\web-clients\joseph-sardella\jpsrealtor\docs\mobile\agent-guides\AGENT_1_SCAFFOLD.md` through `AGENT_8_CAMPAIGNS_ANALYTICS.md` | no | Eight agent guides exist (scaffold, shared package, conversion script, listings/search, map, chat/messaging, CRM, campaigns). No `mobile/` directory in the repo yet. |
| Campaign panel refactor (split 1,324-line `CampaignDetailPanel.tsx` into pipeline wizard + per-tab components < 300 lines) | `F:\web-clients\joseph-sardella\jpsrealtor\docs\refactoring\CAMPAIGN_PANEL_REFACTORING.md` | unclear | Plan describes target architecture; verify against current `src/components/campaigns/CampaignDetailPanel.tsx` line count before treating as outstanding. |
| Package cleanup (remove 20 unused npm packages, ~200-300 MB) | `F:\web-clients\joseph-sardella\jpsrealtor\docs\optimization\PACKAGE_CLEANUP_ANALYSIS.md` | unclear | Dated Feb 6 2026. Three.js ecosystem (`@react-three/drei`, `@react-three/fiber`, `three`, `maath`) listed as removable; verify against current `package.json`. |
| ChatRealty hub-and-spoke domain network (cross-linking SEO, multi-site agent presences) | `docs\multi-tenant\domain-mapping.md` + MEMORY.md | partial | Domain Registry exists; cross-linking and agent custom domains work on the resolver side. The network effects (shared lead pool, cross-site SEO juice) aren't fully realized yet. |
| Database sessions (replace JWT strategy) | (mentioned in tech-debt above) | no | Aspirational. No `Session` collection yet. |

---

## See also

- `F:\web-clients\joseph-sardella\jpsrealtor\docs-v2\ARCHITECTURE.md` — cross-cutting invariants list (linked from "Known tech debt" section)
- `F:\web-clients\joseph-sardella\jpsrealtor\docs-v2\auth\README.md` § Gotchas — auth-specific debt
- `F:\web-clients\joseph-sardella\jpsrealtor\docs-v2\routing\README.md` § Gotchas — proxy-specific debt
- `F:\web-clients\joseph-sardella\jpsrealtor\docs-v2\multi-tenant\README.md` § Gotchas — scoping-specific debt
